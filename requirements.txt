# Ubuntu/Linux setup for the CLI plus the optional desktop GUI.
#
# Usage:
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# This installs the core package in editable mode with the data workflow extras
# normally needed to exercise the pipeline and open the GUI. The MACE/Torch stack
# is intentionally not included here because the correct Torch wheel depends on
# CPU/CUDA hardware; install it separately, then run:
#   python -m pip install -e ".[mlp]"
-e .[gui,mp,condense,wbm,sqs,phonon]

# Development tools used by this repository.
pytest>=7
ruff
black
tomli>=2; python_version < "3.11"
